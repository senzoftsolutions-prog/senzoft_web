import { ArrowUpRight, MapPin } from 'lucide-react'
import { Link } from 'react-router-dom'
import { CTA } from '../components/sections/CTA'
import { PageHero } from '../components/ui/PageHero'
import { Seo } from '../components/ui/Seo'
import { contentRepository } from '../content/repository'
export default function CareersPage(){const jobs=contentRepository.getJobs();return <><Seo title="Careers | SENZOFT" description="Explore opportunities to create impact at SENZOFT."/><PageHero eyebrow="Careers" title="Do work that moves ideas forward." description="Join a team where curiosity, ownership and craft turn meaningful problems into useful technology."/><section className="section"><div className="container-shell"><div className="grid gap-5 md:grid-cols-3">{[['Learn continuously','Grow through useful feedback and challenging work.'],['Build together','Work across disciplines with openness and respect.'],['Own the outcome','Connect your craft to the impact it creates.']].map(([a,b])=><div key={a} className="card p-7"><h2 className="text-2xl font-bold">{a}</h2><p className="mt-3 leading-7 text-brand-muted">{b}</p></div>)}</div><div className="mt-20 flex items-end justify-between"><div><span className="eyebrow">Open roles</span><h2 className="display mt-5 text-5xl">Find your next challenge.</h2></div></div><div className="mt-9 divide-y divide-black/10 border-y border-black/10">{jobs.map(job=><Link key={job.slug} to={`/careers/${job.slug}`} className="group grid gap-3 py-7 md:grid-cols-[1fr_auto_auto] md:items-center"><div><span className="text-xs font-bold text-brand-orange">{job.department} · Preview</span><h3 className="mt-2 text-2xl font-bold group-hover:text-brand-orange">{job.title}</h3></div><span className="flex items-center gap-2 text-sm text-brand-muted"><MapPin size={16}/>{job.location}</span><ArrowUpRight/></Link>)}</div></div></section><CTA/></>}
